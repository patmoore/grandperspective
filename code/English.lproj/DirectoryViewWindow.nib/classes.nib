{
    IBClasses = (
        {CLASS = DirectoryView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {
                colorMappingChanged = id; 
                colorPaletteChanged = id; 
                downAction = id; 
                editMask = id; 
                freeSpaceCheckBoxChanged = id; 
                maskCheckBoxChanged = id; 
                openFileInFinder = id; 
                upAction = id; 
            }; 
            CLASS = DirectoryViewControl; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                colorMappingPopUp = NSPopUpButton; 
                colorPalettePopUp = NSPopUpButton; 
                downButton = NSButton; 
                drawer = NSDrawer; 
                fileSizeMeasureField = NSTextField; 
                filterDescriptionTextView = NSTextView; 
                filterNameField = NSTextField; 
                freeSpaceCheckBox = NSButton; 
                freeSpaceField = NSTextField; 
                itemPathField = NSTextField; 
                itemSizeField = NSTextField; 
                mainView = DirectoryView; 
                maskCheckBox = NSButton; 
                maskDescriptionTextView = NSTextView; 
                openButton = NSButton; 
                scanTimeField = NSTextField; 
                selectedItemExactSizeField = NSTextField; 
                selectedItemPathTextView = NSTextView; 
                selectedItemSizeField = NSTextField; 
                selectedItemTitleField = NSTextField; 
                treePathTextView = NSTextView; 
                treeSizeField = NSTextField; 
                upButton = NSButton; 
                visibleFolderExactSizeField = NSTextField; 
                visibleFolderPathTextView = NSTextView; 
                visibleFolderSizeField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = DummyView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {abort = id; }; 
            CLASS = StartupControl; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                progressIndicator = NSProgressIndicator; 
                progressPanel = NSPanel; 
                progressText = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}